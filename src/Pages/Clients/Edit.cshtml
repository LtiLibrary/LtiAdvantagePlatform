@page
@model EditModel

@{
    ViewData["Title"] = "Clients";
}

<div class="container">
    <h4>Edit Client</h4>
    <hr />
    <div class="row">
        <div class="col-6">
            <form method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="MyClient.UserId" />
                <div class="form-group">
                    <label asp-for="MyClient.Id"></label>
                    <input asp-for="MyClient.Id" class="form-control" readonly="readonly" />
                </div>
                <div class="form-group">
                    <label asp-for="MyClient.Name"></label>
                    <input asp-for="MyClient.Name" class="form-control" />
                    <span asp-validation-for="MyClient.Name" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" value="Save" class="btn btn-secondary" />
                </div>
            </form>
            <div>
                <a asp-page="./Index">Back to List</a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
