@page
@model DetailsModel

@{
    ViewData["Title"] = "Details";
}

<div class="container">
    <h4>Tool</h4>
    <hr />
    <dl>
        <dt>
            @Html.DisplayNameFor(model => model.Tool.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Tool.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Tool.Url)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Tool.Url)
        </dd>
    </dl>
    <div>
        <a asp-page="./Edit" asp-route-id="@Model.Tool.Id">Edit</a> |
        <a asp-page="./Index">Back to List</a>
    </div>
</div>
